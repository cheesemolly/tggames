// Особые скины (перки): владелец выдаёт их отдельным игрокам в панели (карточка игрока → «Особые скины»).
// Кому что выдано — хранится на сервере (server/lib.js: PERKS, таблица user_perks), а не в коде: репозиторий
// публичный, id игроков в нём быть не должно. Владелец видит все перки и так (сервер отдаёт их в /me).
// Игра спрашивает api.perk('<id>'): показать ли особый скин этому игроку. Список сверяется тестом с сервером.

export const PERKS = {
  hedgehog: 'Фон «Ёжик в цветах» — судоку и Block Blast',
};
