{
  "name": "tggames",
  "private": true,
  "type": "module",
  "description": "Только для тестов (node --test). Зависимостей и сборки нет.",
  "scripts": {
    "start": "python -m http.server 8000",
    "test": "node --test"
  }
}
