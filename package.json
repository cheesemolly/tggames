{
  "name": "tggames",
  "private": true,
  "type": "module",
  "description": "Только для тестов (node --test). Зависимостей и сборки нет.",
  "scripts": {
    "start": "python tools/serve.py 8000",
    "test": "node --test"
  }
}
